[project]
name = "oscar-db"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "anthropic>=0.120.2",
    "pandas>=3.0.3",
]

[dependency-groups]
dev = [
    "datasette>=0.65.2",
    "ipykernel>=7.3.0",
]
